---
interface Props {
  className?: string;
  hover?: boolean;
}

const { className = '', hover = true } = Astro.props;
---

<div class={`surface-elevated overflow-hidden ${hover ? 'hover:-translate-y-0.5 hover:shadow-xl transition-all duration-200' : ''} ${className}`}>
  <slot />
</div>
