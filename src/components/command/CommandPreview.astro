---
import type { Command } from '../../data/commands';
import Card from '../ui/Card.astro';
import Badge from '../ui/Badge.astro';

interface Props {
  command?: Command;
}

const { command } = Astro.props;
---

<aside id="resizable-aside" class="hidden lg:block w-80 p-6 bg-white border-l border-gray-200 overflow-y-auto relative" style="min-width: 280px; max-width: 600px;">
  <!-- Borde de redimensionamiento -->
  <div id="resize-handle" class="absolute left-0 top-0 w-2 h-full cursor-col-resize transition-colors group flex items-center justify-center">
    <div class="w-0.5 h-full bg-gray-300 group-hover:bg-blue-500 transition-colors"></div>
    <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 opacity-0 group-hover:opacity-100 transition-opacity">
      <div class="flex flex-col space-y-0.5">
        <div class="w-1 h-1 bg-blue-500 rounded-full"></div>
        <div class="w-1 h-1 bg-blue-500 rounded-full"></div>
        <div class="w-1 h-1 bg-blue-500 rounded-full"></div>
      </div>
    </div>
  </div>

  <div class="flex justify-between items-center mb-4">
    <h2 class="text-xl font-bold text-gray-800">Detalles del comando</h2>
    <div class="flex items-center space-x-2">
      <button 
        class="text-gray-400 hover:text-blue-600 focus:outline-none" 
        onclick="resetAsideSize()"
        title="Resetear tamaño"
      >
        <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
        </svg>
      </button>
      <button onclick="clearPreview()" class="text-gray-400 hover:text-gray-600 focus:outline-none">
        <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
        </svg>
      </button>
    </div>
  </div>

  <!-- Estado inicial -->
  <div id="preview-placeholder" class="text-center py-8">
    <div class="bg-gradient-to-br from-blue-50 to-indigo-50 rounded-lg p-6 mb-6">
      <div class="text-blue-500 mb-4">
        <svg class="h-12 w-12 mx-auto" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M8 9l3 3-3 3m5 0h3M5 20h14a2 2 0 002-2V6a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
        </svg>
      </div>
      <h3 class="text-lg font-semibold text-gray-800 mb-2">¡Bienvenido!</h3>
      <p class="text-gray-600 text-sm mb-4">Haz clic en cualquier comando para ver sus detalles, ejemplos y parámetros.</p>
      <div class="text-xs text-blue-600 bg-blue-100 rounded-full px-3 py-1 inline-block">
        💡 Tip: Puedes redimensionar este panel
      </div>
    </div>

    <!-- Comandos populares -->
    <div class="text-left">
      <h4 class="text-sm font-semibold text-gray-700 mb-3">🔥 Comandos populares</h4>
      <div class="space-y-2">
        <div class="flex items-center justify-between p-2 bg-gray-50 rounded text-xs hover:bg-gray-100 transition-colors cursor-pointer" onclick="showPreview('ls')">
          <span class="font-mono text-blue-600">ls -la</span>
          <span class="text-gray-500">Lista archivos</span>
        </div>
        <div class="flex items-center justify-between p-2 bg-gray-50 rounded text-xs hover:bg-gray-100 transition-colors cursor-pointer" onclick="showPreview('grep')">
          <span class="font-mono text-green-600">grep</span>
          <span class="text-gray-500">Buscar texto</span>
        </div>
        <div class="flex items-center justify-between p-2 bg-gray-50 rounded text-xs hover:bg-gray-100 transition-colors cursor-pointer" onclick="showPreview('ssh')">
          <span class="font-mono text-indigo-600">ssh</span>
          <span class="text-gray-500">Conexión remota</span>
        </div>
      </div>
    </div>
  </div>

  <!-- Contenido del preview -->
  <div id="preview-content" class="hidden space-y-6">
    <div>
      <h3 class="font-semibold text-gray-700 mb-1">Comando</h3>
      <code id="preview-comando" class="block font-mono bg-gray-100 p-2 rounded text-blue-700"></code>
    </div>

    <div>
      <h3 class="font-semibold text-gray-700 mb-1">Sintaxis</h3>
      <code id="preview-sintaxis" class="block font-mono bg-gray-100 p-2 rounded text-gray-800"></code>
    </div>

    <div>
      <h3 class="font-semibold text-gray-700 mb-1">Descripción</h3>
      <p id="preview-descripcion" class="text-gray-700"></p>
    </div>

    <div id="preview-warning" class="hidden">
      <div class="bg-red-50 border-l-4 border-red-400 p-4">
        <div class="flex">
          <div class="flex-shrink-0">
            <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
            </svg>
          </div>
          <div class="ml-3">
            <p class="text-sm text-red-700">
              ⚠️ Este comando puede ser peligroso si no se usa correctamente
            </p>
          </div>
        </div>
      </div>
    </div>

    <div>
      <h3 class="font-semibold text-gray-700 mb-2">Ejemplo rápido</h3>
      <div id="preview-ejemplo" class="font-mono bg-gray-800 text-green-400 p-3 rounded"></div>
    </div>

    <div>
      <h3 class="font-semibold text-gray-700 mb-2">Tags</h3>
      <div id="preview-tags" class="flex flex-wrap gap-2"></div>
    </div>
  </div>
</aside>

<script>
// Resize functionality
let isResizing = false;
let startX = 0;
let startWidth = 0;

const aside = document.getElementById('resizable-aside');
const resizeHandle = document.getElementById('resize-handle');

if (resizeHandle && aside) {
  resizeHandle.addEventListener('mousedown', (e) => {
    isResizing = true;
    startX = e.pageX;
    startWidth = aside.offsetWidth;
  });

  document.addEventListener('mousemove', (e) => {
    if (!isResizing) return;

    const width = startWidth + (startX - e.pageX);
    
    // Límites min/max
    if (width >= 280 && width <= 600) {
      aside.style.width = `${width}px`;
      localStorage.setItem('asideWidth', width.toString());
    }
  });

  document.addEventListener('mouseup', () => {
    isResizing = false;
  });

  // Prevenir selección de texto mientras se redimensiona
  resizeHandle.addEventListener('selectstart', (e) => {
    e.preventDefault();
  });

  // Restaurar ancho guardado
  const savedWidth = localStorage.getItem('asideWidth');
  if (savedWidth) {
    aside.style.width = `${savedWidth}px`;
  }
}

function resetAsideSize() {
  if (aside) {
    aside.style.width = '320px';
    localStorage.removeItem('asideWidth');
  }
}

function clearPreview() {
  const placeholder = document.getElementById('preview-placeholder');
  const content = document.getElementById('preview-content');
  if (placeholder) placeholder.classList.remove('hidden');
  if (content) content.classList.add('hidden');
}

// Declarar globalmente
(window as any).resetAsideSize = resetAsideSize;
(window as any).clearPreview = clearPreview;
</script>
